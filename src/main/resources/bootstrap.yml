spring:
  application:
    name: gamma-gateway-service
  cloud:
    config:
      uri: ${CONFIG_URI:}
      username: ${CONFIG_USERNAME:}
      password: ${CONFIG_PASSWORD:}
      label: ${CONFIG_GIT_BRANCH:}